{
  "name": "OSX docker",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "runArgs": [
    "--rm",
    "-p", "9000:8006",
    "--device=/dev/kvm",
    "--cap-add=NET_ADMIN",
    "--stop-timeout", "120"
  ]
}
